# Демонстрационные данные

Проект уже содержит тестовые данные в JSON файлах.

## Как увидеть данные:

### 1. Запустите бэкенд:
```bash
cd backend
python -c "import uvicorn; uvicorn.run('main:app', host='0.0.0.0', port=8001)"